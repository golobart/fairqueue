pipeline {
    agent {
        docker { image 'node:7-alpine'
                 args '-u root:sudo' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --version'
            }
        }
    }
}
